# local venv / node
.venv/
node_modules/
__pycache__/
*.pyc

# runtime data
backend/uploads/
logs/
log/

# local secrets - .env plus any backup/variant (.env.bak.*, .env.local, ...)
.env
.env.*
!.env.example

# backups made during install (originals live in git history)
*.orig
